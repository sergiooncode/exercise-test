{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "exercisestestdbuser",
  "password": "password",
  "database": "exercisestestdb",
  "synchronize": true,
  "logging": false,
  "entities": ["dist/**/*.entity.{ts,js}"],
  "migrations": ["dist/migration/*.{ts,js}"],
  "cli": {
    "entities": "dist/**/*.entity.js",
    "migrationsDir": "dist/migration"
  }
}
